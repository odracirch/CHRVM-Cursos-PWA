'use client'; import {useEffect,useState} from 'react'; import CourseCard from '@/components/CourseCard'; import {api} from '@/lib/api';
export default function Courses(){const [data,setData]=useState<any[]>([]); const [q,setQ]=useState(''); const [loading,setLoading]=useState(true); useEffect(()=>{api('/api/courses/').then(d=>setData(d.results||d)).catch(()=>setData([])).finally(()=>setLoading(false))},[]); const filtered=data.filter(c=>(c.title+' '+c.short_description).toLowerCase().includes(q.toLowerCase())); return <div className="max-w-6xl mx-auto px-4 py-10"><h1 className="text-3xl font-black">Catálogo de cursos</h1><p className="text-slate-600 mt-2">Encuentra formación y comienza a avanzar.</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar cursos..." className="w-full md:w-96 border rounded-xl p-3 mt-6"/>{loading?<p className="py-10">Cargando...</p>:<div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5 mt-8">{filtered.map(c=><CourseCard key={c.id} c={c}/>)}</div>}</div>}
