'use client'; import {useState} from 'react'; import {login} from '@/lib/api'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const r=useRouter(); async function go(e:any){e.preventDefault();try{await login(email,password);r.push('/dashboard')}catch(x:any){setErr(x.message)}}return <div className="max-w-md mx-auto px-4 py-14"><div className="card p-7"><h1 className="text-2xl font-black">Ingresar</h1><form onSubmit={go} className="space-y-4 mt-6"><input className="w-full border rounded-lg p-3" placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="w-full border rounded-lg p-3" placeholder="Contraseña" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="w-full bg-brand-600 text-white rounded-lg p-3">Entrar</button></form>{err&&<p className="text-red-600 text-sm mt-3">{err}</p>}<p className="text-sm mt-5">¿No tienes cuenta? <Link className="text-brand-600" href="/registro">Regístrate</Link></p></div></div>}
