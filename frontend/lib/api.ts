export const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000';
export async function api(path:string,options:RequestInit={}){const token=typeof window!=='undefined'?localStorage.getItem('chrvm_access'):null; const headers=new Headers(options.headers); headers.set('Content-Type','application/json'); if(token)headers.set('Authorization',`Bearer ${token}`); const res=await fetch(`${API}${path}`,{...options,headers,cache:'no-store'}); if(!res.ok){let msg='Error de API'; try{const d=await res.json(); msg=d.detail||JSON.stringify(d)}catch{} throw new Error(msg)} return res.status===204?null:res.json()}
export async function login(email:string,password:string){const r=await api('/api/auth/login/',{method:'POST',body:JSON.stringify({email,password})}); localStorage.setItem('chrvm_access',r.access); localStorage.setItem('chrvm_refresh',r.refresh); return r}
